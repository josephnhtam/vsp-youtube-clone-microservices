import {DragAndDrogFileDirective} from './drag-and-drog-file.directive';

describe('DragAndDrogFileDirective', () => {
  it('should create an instance', () => {
    const directive = new DragAndDrogFileDirective();
    expect(directive).toBeTruthy();
  });
});
