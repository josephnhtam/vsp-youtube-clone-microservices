apiVersion: v1
kind: Service
metadata:
  name: subscriptions-api
  labels:
    app: subscriptions-api
spec:
  selector:
    app: subscriptions-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: http